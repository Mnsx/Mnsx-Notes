# 散列表

## 散列函数

散列函数就是**将输入映射到数字**的函数，散列函数必须是**一致的**，并且它**应该将不同的输入映射到不同的数字**

给两个键分配的位置相同，这种情况称为冲突，处理冲突的方式最简单的就是，**如果两个键映射到了同一个位置，就在这个位置存储一个链表**

散列表中散列函数非常重要，最理想的情况是，**散列函数将键均匀地映射到散列表的不同位置**

## 填装因子

散列表的填装因子计算方式为：**散列表包含的元素数除以位置总数**

如果**填装因子大于0.7**，以为着存储的数据个数大于数组的长度了，那么就应该进行**调整长度**了